<% if @feed_items.any? %>
    <ol class="microposts">
        <%= @feed_items.each do |f| %>
            <%= render f %>
            <%= link_to "リツイート", retweets_micropost_id(f),{confirm: <= "リツイートしますか"} %>
    </ol>
<% end %>